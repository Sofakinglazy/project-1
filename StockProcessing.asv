%% Process Stock Data 
% Open the folder 
% Count the numbder of .csv files
% Load all the files 
% Calculate return rate for each stock
% Merge them into a matrix 

function stock
currDir = pwd;
stockDir = dir([currDir, '\stockPrice']);
numFiles = length(stockDir(not([stockDir.isdir])));

for i = 1:numFiles
    load(['stock_', i, ''])

